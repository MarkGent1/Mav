{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApplicationInsights": {
    "AppName": "Mav.EventListener",
    "InstrumentationKey": "Value set in kubernetes,tf, launchSettings or docker-compose for local dev."
  },
  "ServiceBusConfiguration": {
    "FullyQualifiedNamespace": "Value set in kubernetes,tf, launchSettings or docker-compose for local dev."
  },
  "AccountApiConfiguration": {
    "BaseUrl": "Value set in kubernetes,tf, launchSettings or docker-compose for local dev.",
    "ResiliencePolicy": {
      "Retries": 3,
      "MaxJitter": 50
    }
  },
  "CustomerApiConfiguration": {
    "BaseUrl": "Value set in kubernetes,tf, launchSettings or docker-compose for local dev.",
    "ResiliencePolicy": {
      "Retries": 3,
      "MaxJitter": 50
    }
  }
}
